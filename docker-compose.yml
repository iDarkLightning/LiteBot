version: "3.9"
services:
  bot:
    build: .
    volumes:
    - "./logs:/bot/logs"
    - "./config:/bot/config"
    - "C:/Users/nirjh/Desktop/test server:/bot/servers/smp"
    environment:
      PYTHONUNBUFFERED: 1
    ports:
      - ${SERVER_PORT}:8080